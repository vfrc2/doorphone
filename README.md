# doorphone

linphone base dorphone

<!-- inject -->
```
Usage: doorphone [OPTION...]
Auto answer phone to use as doorphone, can be run as daemon

  -?, --help                 Give this help list
      --usage                Give a short usage message
  -V, --version              Print program version


To call dest phones can send SIGUSR1 signal to process
Use env for write process pid into file
DOORPHONE_PID_FILE=/var/doorphone.pid

Report bugs to <vfrc29@gmail.com>.
```
<!-- end -->